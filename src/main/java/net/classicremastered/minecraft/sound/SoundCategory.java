package net.classicremastered.minecraft.sound;

/**
 * Categories allow you to group sounds and control them separately
 * (master volume, mobs, blocks, ambient, UI, etc).
 */
public enum SoundCategory {
    MASTER,
    BLOCKS,
    MOBS,
    AMBIENT,
    WEATHER,
    PLAYER,
    UI,
    MUSIC
}
